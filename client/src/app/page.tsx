import { Post } from '@/components/Posts/Post.components';

export default function Home() {
    return (
        <main className="flex flex-col mt-2 items-center">
            <Post />
        </main>
    );
}
